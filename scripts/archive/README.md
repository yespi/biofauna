# Archived scripts

Not part of the reconstruction path.

| File | Why archived |
|------|----------------|
| `embed_crop_yolo_legacy.py` | YOLO organism crop + BioCLIP-2 (768-d). Production uses full-frame ViT-H and 65% ROI fusion, not this detector. |
| `negative/train_qlora.py` | QLoRA path that failed or never beat frozen k-NN. |
| `negative/train_triplet.py` | Triplet on ViT-H degraded accuracy. |
